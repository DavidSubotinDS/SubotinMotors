# Explicit local diagnostics only; normal compose.yaml publishes none of these.
services:
  backend:
    ports: ["127.0.0.1:18080:8080"]
  frontend:
    ports: ["127.0.0.1:15173:8080"]
  mysql:
    ports: ["127.0.0.1:13306:3306"]
